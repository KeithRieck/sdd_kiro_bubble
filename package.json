{
  "name": "bubble-consumption-game",
  "version": "1.0.0",
  "description": "A browser-based PWA game where the player controls a bubble that grows by consuming smaller bubbles",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jsdom": "^28.1.0",
    "vitest": "^1.0.0"
  }
}
